module.exports = {
    purge: {
      enabled: process.env.NODE_ENV === 'production',
      content: [
        './templates/**/*.html',
        './static/js/**/*.js',
      ],
      options: {
        safelist: [
          /^admin-/,  // Keep admin related classes
        ],
      },
    },
    darkMode: false, // or 'media' or 'class'
    theme: {
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
  };
